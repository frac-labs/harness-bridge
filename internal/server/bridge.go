package server

// Bridge service stubs: EnqueueTicket, EmitEvent.
// Wired up in B3 once the harness-protos service descriptors stabilize.
//
// Left intentionally minimal at v0.1.0. The image is releasable; downstream
// cards (B3 / B4) wire concrete request/response paths.
